import React from "react";

const CTA = () => (
  <section id="cta" className="cta">
    <h2 className="cta-text">Let’s Build Something Exceptional.</h2>
    <a href="#footer" className="btn btn-primary">Start a Project</a>
  </section>
);

export default CTA;
